# FreePalp — модель угроз

FreePalp — мульти-агентный оркестратор, который автономно вызывает инструменты
(файлы, shell, web), хранит память и **модифицирует собственный код**. Это
расширяет поверхность атаки за пределы обычного чат-приложения. Документ
перечисляет угрозы, защиты и непокрытые риски честно.

## Активы под защитой
- Машина пользователя (файлы вне песочницы, ОС).
- API-ключи провайдеров (`.env`, in-memory).
- Исходный код FreePalp (самомодификация → возможность закрепиться).
- Память (HOT/WARM/COLD, векторный индекс) — может быть носителем инъекций.

## Границы доверия
- **Доверенное:** код в репозитории, сам пользователь.
- **Недоверенное:** вывод LLM, содержимое веб-страниц, прикреплённые файлы,
  ранее записанная память (может содержать инъекцию из прошлой сессии).

## Угрозы и защиты

### T1. Path traversal — выход за песочницу
Атака: `../../../etc/passwd`, абсолютные пути, `..\` на Windows.
Защита: `_safe_path()` нормализует, `resolve()` + проверка `SANDBOX_ROOT in
parents`. Абсолютные пути и литеральные `....` удерживаются внутри песочницы.
Покрытие: `test_security.py::test_path_traversal` (12 кейсов).

### T2. Обход shell-whitelist
Атака: цепочки `cmd1; cmd2`, `&&`, `|`, подстановка `$(...)`/`` `...` ``,
редиректы `>`/`<`.
Защита: whitelist базовой команды + явный запрет метасимволов (проверяется вся
строка, не только первый токен). Покрытие: `test_security.py::test_shell_whitelist`.

### T3. Python-эксплойт через интерпретатор
Атака: `python -c "import os; os.system(...)"`, запуск скрипта вне песочницы.
Защита: `python -c` запрещён; py-файлы исполняются только из песочницы.
Покрытие: `test_security.py::test_python_exploit`.

### T4. Prompt-injection через память/файлы/веб
Атака: «Ignore all previous instructions», подставные `SYSTEM:`-блоки,
`<|im_start|>` в содержимом, которое попадёт в контекст агента.
Защита (частичная): детерминированные пост-проверки результата (детектор
галлюцинаций файлов, идентичности, сырых tool_call), не доверяющие тексту LLM;
распознавание маркеров в `test_security.py::test_injection_markers`.
**Непокрыто полностью:** активной санации содержимого памяти/веба перед
инжекцией в промпт пока нет — это известный остаточный риск (см. ниже).

### T5. Заглушки/потеря данных при операциях с файлами
Атака/баг: LLM «копирует» файл, печатая плейсхолдер → потеря данных.
Защита: `copy_file` (детерминированная копия) + детектор заглушек в `write_file`.

### T6. Утечка ключей
Защита: `.env` в `.gitignore`; история git проверена — реальных ключей нет,
только плейсхолдеры в `.env.example`. Гостевой режим изолирует чужие ключи.

## Остаточные риски (честно, не закрыто)
1. **Санация инъекций в памяти/вебе** — маркеры распознаём, но не вычищаем
   автоматически перед попаданием в контекст. Приоритет до широкой публикации.
2. **Самомодификация** — агент пишет в собственные исходники (`write_source`).
   Ограничено директорией `freepalp/`, но злонамеренный/ошибочный патч теоретически
   может ослабить защиты. Митигировано гейтом test_mvp + held-out перед активацией
   версии и git-историей для отката; формального code-review патча нет.
3. **Зависимости** — нет автоматического аудита (pip-audit/Dependabot).

## Как сообщить об уязвимости
Issue с тегом `security` или личным сообщением мейнтейнеру. Не публикуйте
рабочий эксплойт до фикса.
