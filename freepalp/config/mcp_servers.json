{
  "_comment": "MCP-серверы (Model Context Protocol). Добавь сервер — его инструменты появятся у воркера автоматически под именем mcp__<server>__<tool>. Формат как у Claude Desktop. Пример (перенеси в mcpServers и впиши свой путь):",
  "_example": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/path/to/your/folder"
      ],
      "env": {}
    }
  },
  "mcpServers": {}
}