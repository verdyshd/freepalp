# Секреты
.env

# Кэши и логи
__pycache__/
*.pyc
.pytest_cache/
*.log
logs/

# Рабочие данные агента (не код системы)
freepalp/memory/*
!freepalp/memory/*.py
freepalp/sandbox/
freepalp/state/
freepalp/logs/

# Прочее
node_modules/
.claude/

# Личные рабочие файлы и разовые логи
всякое/
server_*.txt
tunnel_*.txt
*.log

# Бенчмарк-прогоны (runtime)
benchmarks/

# Рантайм-логи сервера
server_log.txt
server_err.txt
server_*.txt

# Dev-скрипты и локальные доки (не в публичный репо)
train_*.py
ИНСТРУКЦИЯ.md

# Артефакты сборки/упаковки (pip install -e .)
build/
dist/
*.egg-info/

# Локальные (приватные) инструменты — не в публичный репо
freepalp/tools/reddit_tools.py

# Эвал-прогоны (runtime); публикуемые eval/scorecard.md и eval/hard_tasks.json добавлять явно (git add -f)
eval/*.json

# QA-артефакты сессий (скрипты проверок, скриншоты, дампы) — не публикуются
_qa/
